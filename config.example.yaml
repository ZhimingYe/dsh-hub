# First `dsh-hub serve` writes bcrypt hashes. Add users with `dsh-hub hash`.
port: 8787
agentSecret: "$2a$12$....................................................."
users:
  alice: "$2a$12$....................................................."
